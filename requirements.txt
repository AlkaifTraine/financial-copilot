# Financial Copilot - runtime dependencies
#
# NOTE: this file is deliberately free of `torch` / `sentence-transformers`.
# Embeddings are served by the OpenAI API, which keeps the deployed image
# small enough for Streamlit Community Cloud's 1 GB memory ceiling.

# ---- App shell ----
streamlit==1.57.0
python-dotenv==1.2.2

# ---- LLM + RAG ----
# Model calls go through a LiteLLM Router (fincopilot/llm.py) for cross-provider
# fallback and load balancing; the OpenAI SDK is still the transport it uses for
# OpenAI deployments and is what serves embeddings directly.
litellm==1.80.7
openai==2.37.0
langchain-core==1.4.0
langchain-openai==1.2.1
langchain-text-splitters==1.1.2
tiktoken==0.13.0
faiss-cpu==1.13.2
rank-bm25==0.2.2

# ---- Document discovery + ingestion ----
requests==2.34.2
beautifulsoup4==4.14.3
lxml==6.1.1
ddgs==9.14.4
PyMuPDF==1.27.2.3

# ---- Fundamentals + market data ----
yfinance==1.3.0
yahooquery==2.4.1
pandas==3.0.3
numpy==2.4.6

# ---- Report rendering ----
Jinja2==3.1.6
matplotlib==3.10.9
reportlab==4.5.1
